{% extends 'base.html.twig' %}

{% block title %}NiveauApprentissage{% endblock %}

{% block body %}
    <h1>NiveauApprentissage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_apprentissage.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ niveau_apprentissage.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adminListeNiveauApprentissage') }}">back to list</a>

    <a href="{{ path('adminModifierUnNiveauApprentissage', {'id': niveau_apprentissage.id}) }}">edit</a>

    {{ include('niveau_apprentissage/_delete_form.html.twig') }}
{% endblock %}
